version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-express-app
    container_name: my-express-container
    restart: always
    ports:
      - 80:8000
    environment:
      MONGO_URL: mongodb://0.0.0.0:27017/grocery
    depends_on:
      - mongo
  mongo:
    image: mongo:latest
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data: